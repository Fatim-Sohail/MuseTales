import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  ul: {
    justifyContent: 'center',
    left: '50%',
    marginTop: '30px',
    marginBottom: '30px',
    alignItems: 'center',
    alignSelf: 'center',
  },
}));